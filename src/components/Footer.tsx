import React from 'react'
import styled from '@emotion/styled'

const TheFooter = styled.footer`
  min-height: 100vh;
  background-color: rgba(66, 151, 203, 0.85);
`
const Footer = () => {
  return <TheFooter></TheFooter>
}

export default Footer
